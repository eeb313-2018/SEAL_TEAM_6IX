---
title: "Nov 22 Data Analysis"
author: "Richard"
date: "November 22, 2018"
output: html_document
---

# Load Data

```{r}
library(tidyverse)

swallows <- read_csv("Swallows Data Expanded.csv")

swallows <- swallows[,c(139, 1:138, 140:150)]
head(swallows)
```

# Make heatmap -- distribution to year

## Initial exploration (Alex's code)

```{r}
ggplot(birds, aes(x = Longitude, y = Latitude))+
  geom_point(aes(colour = species))+
  facet_wrap(~species)+
  theme(axis.text.x = element_text(size = 9, angle = 45, hjust = 1))
```

## Swallows Distribution by years

Note: More data have been observed since 1994.

(Which one?)

```{r}

# mutate(StartT = ifelse(TempScale == "F", (StartTemp - 32) / 1.8, StartTemp)) %>%

swallows_1994 <- swallows %>%
  filter(Year == 2011, species == "Tree Swallow")

ggplot(swallows_1994, aes(x = Longitude, y = Latitude)) +
  geom_point(aes(color = SpeciesTotal)) +
  facet_wrap(~species) +
  theme_light() +
  theme(axis.text.x = element_text(size = 9, angle = 45, hjust = 1)) +
  scale_color_gradientn(limits = c(0,600),
                       # low = "yellow", high = "red",
                       colours = rainbow(5),
                       name = "Birds Observed at Route")
  
```

```{r}

# mutate(StartT = ifelse(TempScale == "F", (StartTemp - 32) / 1.8, StartTemp)) %>%

swallows_1994 <- swallows %>%
  filter(Year == 2008, species == "Tree Swallow")

ggplot(swallows_1994, aes(x = Longitude, y = Latitude)) +
  geom_point(aes(color = SpeciesTotal)) +
  facet_wrap(~species) +
  theme_light() +
  theme(axis.text.x = element_text(size = 9, angle = 45, hjust = 1)) +
  scale_color_gradient(limits = c(0,100),
                       low = "yellow", high = "red",
                       # colours = rainbow(5),
                       name = "Birds Observed at Route")
```

```{r}
# str(birds$Year)

swallows_1995 <- birds %>% 
  filter(Year == 1995)

swallows_2005 <- birds %>% 
  filter(Year == 2005)

ggplot(swallow_1995, aes(x = Longitude, y = Latitude))+
  geom_point(aes(colour = species))+
  facet_wrap(~species)+
  theme(axis.text.x = element_text(size = 9, angle = 45, hjust = 1))

ggplot(birds_2005, aes(x = Longitude, y = Latitude))+
  geom_point(aes(colour = species))+
  facet_wrap(~species)+
  theme(axis.text.x = element_text(size = 9, angle = 45, hjust = 1))
```

# IDEA to self: gganimate by years
# IDEA: check geom_grid